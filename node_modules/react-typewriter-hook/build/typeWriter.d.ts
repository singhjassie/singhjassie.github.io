export default class TypeWritter {
    private memoWord;
    private nextWord;
    private word;
    private index;
    private eventQueue;
    private dummyQueue;
    private erasing;
    constructor();
    restartTypeWriter(): string;
    /**
     * Main process function
     * typing
     *    |-- writing
     *    |-- erasing
     *    |-- restartWrite
     *
     * @returns
     * @memberof TypeWritter
     */
    typing(): string;
    startTypeWord(str: string): string;
    erase(): string;
    rd(): number;
}
